# AI Prompt Generator

This repository is intentionally minimal so it can be imported into Vercel.

Add your app code (Next.js, Node, static files, etc.) and configure build settings in the Vercel dashboard if needed.
